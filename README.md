其中在GUI目录下，有一份说明文档
Django服务器端功能：
    1、有用户注册、登录、退出；
    2、数据库记录的保存，与数据的新建；
    3、用户的分类管理DjangoAdmin；
    4、用户分了普通用户、商户、终端；
    5、终端用户可以开辟订单号，添加商品价格：
    6、普通用户这里只写了，普通的web页面进行操作。
       查看信息，添加新的商品到指定的订单号下。
       
GUI目录文件：这里是端用tkinter编写的在Ubuntu下：
    1、用requests 登录Djangoe服务器；
    2、用requests 请求新数据；
    3、根据服务器返回的新数据用qrcode 生成二维码手机客户端使用；
    4、定时请求服务器，根据新返回的信息，查询MySQL商品数据库价格，
       并向服务发送；（pyMysql+SQLALchemy）实现：
    5、根据得到的信息，计算并以表格的形式显示在桌面；
       